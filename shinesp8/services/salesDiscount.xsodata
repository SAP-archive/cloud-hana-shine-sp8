service namespace "{{PACKAGE_NAME}}"  { 
    "{{PACKAGE_NAME}}.models::CV_CUSTOMER_DISCOUNT_BY_RANKING_AND_REGION"
    as "salesDiscount"  
	keys generate local "ID"
    aggregates always
    parameters via entity "InputParams"; 
}